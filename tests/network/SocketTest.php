<?php

use prodigyview\network\Socket;

use PHPUnit\Framework\TestCase;

class SocketTest extends TestCase {
	
	
	
	public function testSocket() {
		
		$this->assertTrue(true);
		/*
		$socket = new Socket('ws://demos.kaazing.com/echo-auth', 0);
		$response = $socket->send('Rock it with HTML5 WebSocket');
		
		$server = new Socket('localhost', 8000,array('bind' => true, 'listen' => true));
		$server->startServer('',function($message){
			echo $message;
		}, 'closure');
		echo 'Ok';
		
		
		print_r($response);
		
		exit();
		exit();*/
		
	}
}
